import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

export default function frontierAutoresearch(_pi: ExtensionAPI): void {
  // Commands and tools are registered by later vertical slices.
}
