# Changelog

All notable changes to this package are recorded here.

## Unreleased

### Added

- Initial release of the bounded frontier autoresearch package for generic Git codebases.

### Changed

- `autoresearch_configure` now streams live stage progress (repository verification, evaluator dry-run, probes, guards, and each baseline sample) into the tool row in the terminal UI, so long calibration runs are no longer a silent spinner.
